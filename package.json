{
  "name": "atom-analyzer-pro-root",
  "version": "1.0.0",
  "description": "Root orchestrator for Atom Analyzer Pro full-stack deployment",
  "scripts": {
    "install-deps": "npm install --prefix backend && npm install --prefix frontend",
    "build-frontend": "npm run build --prefix frontend",
    "build": "npm run install-deps && npm run build-frontend",
    "start": "node backend/server.js"
  }
}
